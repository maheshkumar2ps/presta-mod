# AWS S3 Configuration (Required for image uploads)
# Copy this file to .env and fill in your AWS credentials

AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name

# Frontend: S3 image hostname so product listing/detail pages can display S3 images.
# Format: your-bucket.s3.region.amazonaws.com (e.g. your-s3-bucket-name.s3.us-east-1.amazonaws.com)
# Required for Docker build when using S3; for local dev set in frontend/.env.local
NEXT_PUBLIC_S3_IMAGE_HOSTNAME=your-s3-bucket-name.s3.us-east-1.amazonaws.com
